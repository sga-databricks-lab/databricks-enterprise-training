# Production environment deployment specs
targets:
  prod:
    mode: production
    default: false
    workspace:
      host: https://dbc-a73655c8-8153.cloud.databricks.com
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundles/${bundle.name}/${bundle.target}
    variables:
      environment: prod
      catalog: prod
      schema_bronze: bronze
      schema_silver: silver
      schema_gold: gold
      warehouse_id: afb90bfccc65e2e0
      # Set your service principal application ID here
    #   service_principal_name: ""  # e.g., "12345678-1234-1234-1234-123456789abc"
    # run_as:
    #   # Use service principal for production deployments
    #   service_principal_name: "${var.service_principal_name}"